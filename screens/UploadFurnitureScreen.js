import React from 'react'
import { StyleSheet, Text } from 'react-native';

export const UploadFurnitureScreen = () => {
  return (
    <Text style={styles.container} > wow</Text>
  )
}

const styles = StyleSheet.create({
    container: {
      padding:50,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
   });